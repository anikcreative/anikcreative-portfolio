import styled from "styled-components";
import { Colors } from "../../theme/Theme";

export const LightEmphasisText = styled.span`
  color: ${Colors.bright};
`;

export const AccentText = styled.span`
  color: ${Colors.accent};
`;